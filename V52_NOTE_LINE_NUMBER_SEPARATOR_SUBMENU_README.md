# v52 便签行号与分界线二级菜单

- 便签右上角新增“行号开/行号关”按钮，可直接切换是否显示每行行号。
- 设置 - 便签 - 编辑器显示中新增“默认显示每行行号”。
- 便签右键菜单改为二级菜单：插入分界线 -> 长度 / 种类。
- 分界线长度可选 8、14、20、28、40、56、72，也支持自定义 4-80。
- 新增横线、细线、粗线、短横线、星号、等号、点线、波浪线、井号、菱形等分界线种类。
- 右键菜单继续以鼠标位置为锚点，并保留边缘防溢出。
