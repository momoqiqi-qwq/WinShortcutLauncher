# v78 代码结构检查

## 本版完成的结构优化

1. 新建子目录逻辑统一到 `useDirectoryCreator`：
   - 左侧新增按钮。
   - 右键添加项目菜单。
   - 子目录管理菜单。
   - 双击侧栏空白新建。

2. 目录相关纯逻辑拆到 `directoryExperience.ts`：
   - 重名自动编号。
   - 右键菜单模式切换。
   - 子目录数量统计。

3. 新增独立 `NavigationSettingsSection.tsx`，避免继续扩大体验设置页面。

4. `addDirectory` 返回新目录 ID，使“创建后立即进入”不依赖再次按名称查找。

## 仍值得优化的地方

按当前文件行数，风险最高的部分是：

- `App.tsx`：约 746 行，同时负责拖放、快捷键、浮层、提示、贴边暂停和窗口交互。
- `ImageBrowserPanel.tsx`：约 677 行，截图、裁剪、分组和文件操作集中。
- `AreaContextMenu.tsx`：约 384 行，添加项目、网址弹窗、显示设置、排序和标签转换集中。
- `useNoteEditorController.ts`：约 430 行，便签选择、行号、保存和右键插入逻辑仍较集中。

## 下一版建议

推荐 v79 做“全局交互拆分 + 右键菜单模块化”：

- 把 App.tsx 的全局快捷键、浮层管理、浏览器拖入和提示气泡拆成独立 hooks。
- 把 AreaContextMenu 的添加项目、显示设置和标签管理拆成子菜单组件。
- 在此基础上增加“右键菜单项目显示/隐藏与顺序”设置，会更安全，也更符合自由度需求。
