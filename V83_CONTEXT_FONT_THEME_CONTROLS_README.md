# v83 右键菜单、字体与主题控件更新

## 1. 右键菜单归类修正

- “子目录 / 空白处”管理同一套添加项目菜单：右键子目录处于“添加项目”模式时，以及内容区/侧栏空白处，均读取 `areaContextMenuHiddenItems`。
- “子目录管理”只管理重命名、合并、类型切换、清空和删除等目录管理功能。
- 子目录默认打开“添加项目”还是“管理目录”，现已移入“设置 - 右键菜单”。
- 当前分类若不是子目录默认菜单，会显示备用菜单提示，并可一键设为默认菜单。

## 2. 字体设置

新增“设置 - 字体”：

- 常用字体：微软雅黑 UI、Segoe UI、HarmonyOS Sans、MiSans、思源黑体、苹方、霞鹜文楷、Cascadia Mono。
- 自定义字体名称或 CSS 字体栈。
- 生效区域：主界面、设置界面、右键菜单、便签正文。
- 未安装字体会按字体栈自动回退；留空则继续跟随当前主题字体。

新增配置字段：

- `display.fontFamily`
- `display.fontApplyAreas`

## 3. 滑动条和复选框

- range 滑动条使用主题 `--accent` 绘制已完成段和滑块。
- 滑道背景使用主题强调色与面板色混合，切换主题时同步变化。
- 通过 `useThemedFormControls` 自动计算每个 range 的完成百分比。
- checkbox 使用主题强调色、加粗边框、较大勾号和聚焦/悬停发光，深浅主题均更明显。

## 4. 兼容性

- 配置存储版本提升到 14，旧配置会自动补齐字体字段。
- 没有选择自定义字体时，界面行为与 v82 一致。
