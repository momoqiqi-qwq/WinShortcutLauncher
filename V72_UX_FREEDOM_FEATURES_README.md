# v72 自由度、项目功能与体验优化

## 设置新增“体验”

- 记住上次父目录/子目录。
- 记住上次设置分类。
- 紧凑设置分类栏。
- 显示/隐藏空目录操作提示。
- 项目启动成功提示。
- 项目悬停动画。
- 减少动画模式。
- 固定项目优先显示。
- 删除项目、删除目录、清空本页的独立确认开关。
- 快速预设：流畅反馈、性能优先、防误操作。

## 设置体验

- 设置左侧新增分类搜索框。
- `Ctrl+,` 可直接打开设置。
- 数据页新增“清除使用统计”。
- 数据页新增“恢复默认设置（保留项目）”。

## 项目功能

- 右键固定项目到列表前面或取消固定。
- 右键创建当前项目副本。
- 右键复制项目路径或网址。
- 项目成功启动后记录启动次数和最近启动时间。
- 排序新增“最近启动”和“最常使用”。
- 非“自定义顺序”时禁用拖拽排序，避免无效操作。

## 更多 Windows 项目

“添加系统功能”扩展为：

- 基础工具
- 常用位置
- Windows 设置
- 管理工具
- 网络工具
- 辅助工具

新增 Windows Terminal、桌面、下载、文档、启动文件夹、回收站、显示设置、声音设置、蓝牙、启动应用、默认应用、剪贴板、Windows 更新、事件查看器、资源监视器、系统配置、磁盘清理、电源选项、屏幕键盘、放大镜等。

## 兼容与持久化

- 配置持久化版本升级到 4。
- 旧配置自动补齐体验设置、固定状态和使用统计字段。
- 导入/导出配置包含体验设置、固定状态和启动统计。
- 关闭“记住上次页面”后，下次启动从第一个目录打开。

## 构建检查

- `npm ci` 通过。
- `npm run build` 通过。
- 当前环境没有 Cargo，因此 Windows 原生 EXE 仍需在 Windows 机器运行 `npm run tauri:build` 验证。
