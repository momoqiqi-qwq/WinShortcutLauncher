# v55 - 便签行号定位与右键二级菜单修复

本版基于 v54 继续修改：

1. 便签行号点击与空行定位
   - 点击行号会直接定位到该行。
   - 点击超过当前已有内容的行号，会自动补空行到目标行，不需要手动按回车补行。
   - 点击便签正文空白区域时，也会按鼠标所在的可见行自动补空行并把光标放到对应行。

2. 行号与正文对齐
   - 行号高度改为按真实逻辑行测量。
   - 长网址或长文字自动换行时，行号会跟随该逻辑行的实际显示高度，避免后面的行号和正文错位。
   - 便签滚动时，行号栏继续同步滚动。

3. 便签右键二级菜单防溢出
   - “插入分界线”二级菜单改为按 exe 视口固定定位。
   - 靠近右边或底部时会自动换边、上移并限制最大高度。
   - 菜单内容过多时二级菜单内部滚动，不再超出界面。

已执行 npm run build，前端构建通过。当前环境没有 Cargo，未执行 Tauri/Rust 原生编译。
