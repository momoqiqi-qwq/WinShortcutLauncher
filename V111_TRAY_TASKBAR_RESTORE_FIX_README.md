# v0.1.111 托盘恢复 / 任务栏图标稳定性修复

本版集中修复 Windows 主窗口从系统托盘重新显示时的三个问题：任务栏图标丢失、窗口尺寸变小、窗口位置发生偏移。

## 1. 任务栏图标重新稳定显示

- 主窗口继续强制使用 `WS_EX_APPWINDOW`，并移除 `WS_EX_TOOLWINDOW`。
- 每次主窗口从托盘显示时重新设置应用图标。
- 修改扩展窗口样式后调用 `SetWindowPos(..., SWP_FRAMECHANGED)`，让 Windows 立即刷新无标题栏窗口的任务栏资格和图标缓存。
- 原生贴边循环重新找到主窗口时也执行同样的任务栏样式刷新，避免贴边动画后再次丢失任务栏图标。

## 2. 托盘隐藏前保存当前窗口几何信息

隐藏到托盘前记录：

- 当前宽度 / 高度
- 当前 X / Y 位置
- 是否最大化

从托盘点击“显示主窗口”或左键点击托盘图标时，优先恢复这份内存快照，再显示窗口，因此不会因为托盘显示动作改成默认大小或其他历史大小。

该快照只服务于当前运行周期的“隐藏 -> 显示”，不会覆盖 v75 已有的启动 / 关机窗口状态保存文件。

## 3. 托盘显示和贴边状态机解耦

之前主窗口在贴边隐藏 / 停靠状态时，如果同时进入系统托盘，原生贴边线程仍可能保留旧的 `main_rect / restore_rect`。托盘再次显示时，旧恢复矩形可能和 Tauri 的显示动作竞争，从而造成：

- 窗口突然缩小
- 窗口跳到旧位置
- 贴边隐藏位置覆盖正常位置

现在新增托盘专用状态：

- 主窗口进入托盘后，原生贴边线程保持休眠，不再重新显示或移动主窗口。
- 从托盘显示时先清空旧贴边恢复状态，并短暂暂停贴边自动移动。
- 托盘保存的窗口大小 / 位置拥有本次恢复的优先权。
- 恢复完成后，贴边功能继续按原设置正常工作。

## 性能

- 不新增 React 状态和鼠标事件监听。
- 托盘快照仅保存 5 个基础字段，常驻开销可以忽略。
- `SWP_FRAMECHANGED` 只在窗口样式维护 / 托盘恢复时触发，不进入高频 UI 渲染路径。
- 托盘隐藏状态下原生贴边循环反而减少窗口检测和动画操作。
