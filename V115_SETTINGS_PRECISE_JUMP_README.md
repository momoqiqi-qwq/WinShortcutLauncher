# Yue launcher v0.1.115 - 设置搜索精确定位

## 本版目标

修复设置搜索结果点击后只进入“大分类 / 大卡片”，无法直接看到具体设置控件的问题。

典型场景：

- 在设置搜索中输入 `zqd`
- 搜索结果命中“开机自动启动”
- v0.1.114 会进入“操作”分类，但“启动与关闭”仍可能处于折叠状态，因此定位逻辑找不到“开机自启动”复选框，最后只能退回高亮整个“操作行为”区域
- v0.1.115 改为精确定位到该复选框

## 核心修改

### 1. 搜索结果增加精确目标

`SettingsSearchItem` 新增 `targetId`，搜索结果可以同时描述：

- `tab`：进入哪个设置分类
- `section`：需要展开哪个折叠分组
- `targetId`：最终定位到哪个具体设置控件/设置行
- `focusText`：文字匹配的兼容回退

例如“开机自动启动”：

- tab: `behavior`
- section: `launchClose`
- targetId: `autostart`
- focusText: `开机自启动`

### 2. 两阶段定位

点击搜索结果后：

1. 切换到目标设置分类；
2. 找到 `data-settings-section` 对应折叠分组；
3. 如果该分组处于收起状态，自动展开；
4. 等待具体控件渲染；
5. 优先通过 `data-settings-target` 精确定位；
6. 将具体设置行滚动到设置面板中部；
7. 短暂高亮该设置行；
8. 对精确目标，把键盘焦点移动到 checkbox / select / input / button。

只有在没有精确目标时，才继续使用文字匹配和区域级回退。

### 3. `zqd` 精确定位

“开机自动启动”已绑定：

- `data-settings-section="launchClose"`
- `data-settings-target="autostart"`

因此 `zqd -> 开机自动启动` 点击后会直接看到“启动与关闭”里的“开机自启动”复选框。

### 4. 同步接入其它折叠设置

本版同时把精确展开/定位机制接入：

- 单击 / 双击启动
- 浏览器路由中心
- 窗口贴边隐藏
- 开机自动启动
- 彩虹总开关
- 便签行号
- 便签自动保存
- 全局搜索
- 搜索输入防抖
- 文件中转站
- 中转站显示文件图标
- 清空中转站前确认
- 图片浏览设置

“界面”分类原有的 `requestedSection` 展开逻辑继续保留，并与新的控件级定位兼容。

## 兼容性

- 不改变任何用户设置值。
- 不改变拼音搜索规则。
- 不改变折叠分组的手动操作方式。
- 只调整从“设置搜索结果”进入设置项时的导航与定位行为。
