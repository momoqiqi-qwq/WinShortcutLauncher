# v68 版本线整理与低风险恢复

本版重点不是大改界面，而是把之前容易丢失的功能重新整理进当前 v67 代码线，降低后续改动互相覆盖的风险。

## 处理内容

1. 修复当前源码中的低级结构问题
   - 修复 SettingsPanel.tsx 中重复声明 currentTheme。
   - 修复 appStore.ts 中删除子目录逻辑重复声明 directories。
   - 前端重新执行 npm run build 通过。

2. 恢复 设置 - 彩虹
   - 左侧重新加入“彩虹”设置分类。
   - 恢复彩虹总开关。
   - 恢复彩虹鼠标、拖尾、边框、文字设置。
   - 恢复鼠标/边框/文字三套独立颜色条。
   - 先采用稳定版颜色条，后续可再升级为可拖动双竖线精细截取。

3. 恢复提示显示时间
   - 设置 - 界面 新增“提示显示时间”。
   - 底部提示会按该时长自动消失。

4. 恢复 favicon 多来源与测速
   - 设置 - 图标 新增 favicon 获取源选择。
   - 按国内、国外、直连分类。
   - 支持测试域名速度，默认测试 nlpcloud.com。
   - 添加网站/刷新网址图标会使用当前选择的来源和兜底策略。

5. 强化置顶
   - 增加 Windows 原生 SetWindowPos(HWND_TOPMOST / HWND_NOTOPMOST) 命令。
   - 右上角钉子优先调用原生命令，失败时再走 Tauri 兜底。

6. 默认设置恢复
   - 首次打开默认主题使用“浅黑”。
   - 首次打开默认开启彩虹总开关。
   - 已经有本地配置的电脑不会被强制覆盖。

## 构建验证

- 已执行 npm ci
- 已执行 npm run build，前端构建通过
- 当前环境没有 Cargo，未执行 Rust/Tauri 原生编译
