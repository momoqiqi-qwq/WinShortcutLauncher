# V27 便签性能与设置拆分

本版修改：

1. 便签输入性能优化：便签内容先写入本地输入状态，并在 450ms 空闲、失焦、切换标签或组件卸载时提交到全局配置，避免每输入/删除一个字都触发全量配置持久化。
2. 设置页选项折叠：搜索、中转、图片预览设置复用界面设置里的箭头折叠样式，长选项可以展开/收起。
3. 设置页拆分：原“搜索/中转”拆成“搜索”“中转”“图片预览”三个左侧设置板块。

已执行 `npm run build` 验证通过。
