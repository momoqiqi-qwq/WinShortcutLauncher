# v49 - 记住窗口大小与贴边瞬收修复

## 修改内容

1. 设置中新增窗口大小保存开关
   - 设置 - 窗口行为 - 记住主窗口大小和位置
   - 设置 - 窗口行为 - 记住设置窗口大小和位置
   - 开启后，鼠标拖动窗口边框调整后的大小会保存，下次打开按上次大小恢复。
   - 设置面板右下角 resize 的大小现在会通过 ResizeObserver 记录，不再只保存拖动位置。

2. 贴边隐藏收回速度优化
   - 去掉从触发条展开后固定 1.2 秒保护导致的延迟。
   - 鼠标离开主界面后最多只保留 80ms 抖动保护。
   - 当“鼠标移出主界面后隐藏速度”设置为 0ms 时，鼠标离开后立即收回。
   - 直接保留主窗口边缘模式下，收回时继续回到主窗口边缘，不再强制切换成独立触发条模式。

3. 贴边动画速度范围
   - “贴边动画速度”和“鼠标移出主界面后隐藏速度”的滑块最小值改为 0ms。
   - 主窗口贴边隐藏不再强制最低 45ms 动画。

## 版本

- 修改版：v49
- 程序版本：0.1.49
