# V51 便签右键分割线行定位修复

- 修复便签右键插入分割线不是插到所选/右键所在行的问题：右键时会按鼠标在便签里的真实位置计算逻辑行。
- 行定位改为隐藏镜像 textarea 计算，支持自动换行、滚动、不同字体大小、行距和内边距，避免只按固定行高估算导致插错行。
- 分割线插入规则改为占用右键选择的那一行：空行直接替换为分割线，非空行会在该行位置插入分割线并把原内容下移。
- 右键菜单继续以鼠标位置为锚点显示；靠近窗口边缘时只做最小偏移/反向弹出，避免菜单跑远或超出 exe 界面。
- 版本：0.1.51。
