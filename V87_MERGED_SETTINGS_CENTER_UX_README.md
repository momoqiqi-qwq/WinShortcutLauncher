# Yue launcher v87：v85/v86 合并与设置界面统一优化

版本：`0.1.87`

## 合并结果

本版本以 v85 为功能基础，将 v86 的设置排版、文字溢出和简洁模式完整合并，保留两版功能，不再出现“升级后一部分修改丢失”的情况。

### 保留的 v85 功能

- “说明”改为“关于”，仅显示版本、作者 `momoni_yile`、框架和项目地址。
- 设置窗口固定在主界面中央，左侧分类和右侧内容独立滚动。
- 主窗口最低尺寸为 `900 × 640`。
- 主界面和设置界面缩放范围为 `x0.1–x1.8`，步进 `x0.1`。
- 父目录、子目录、空白处使用三套独立右键菜单。
- 子目录菜单只保留空白处菜单没有的管理操作。
- 父目录删除和合并重新接入全局确认弹窗，合并子菜单阻止事件冒泡。
- 设置背景与主界面背景分别配置、分别生效。

### 保留的 v86 功能

- 分段按钮文字不再按单字拆行。
- 设置描述可在“体验”中开关。
- 描述关闭时进入简洁模式，仅保留主要名称和控件。
- 体验页说明文字精简，选项在窄区域自动改为上下布局。
- 选择按钮补充 `aria-pressed`，输入控件补充键盘焦点提示。

## v87 新增优化

1. **所有设置顶层卡片统一水平居中**
   - 同时覆盖 `.settings-section` 和主题页使用的 `.settings-card`。
   - 卡片宽度统一限制在可读范围内，并在窄窗口下使用 100% 可用宽度。

2. **通用窄布局保护**
   - 设置行、快捷键行、路径选择、关于页信息等在窄内容区自动切换为单列。
   - 标题行、按钮行允许安全换行，避免高缩放时挤出卡片。

3. **设置搜索体验**
   - 输入框占位文字缩短为“搜索设置”，避免左侧栏较窄时被截断。
   - 快捷键信息移到悬停提示和无障碍标签中。
   - 无搜索结果时使用状态提示，读屏工具可感知变化。

4. **简洁模式更明确**
   - 开启简洁模式后，设置标题旁显示“简洁模式”标记。
   - 进一步隐藏设置布局说明、窗口状态说明、右键菜单解释等辅助文字。

5. **界面文案精简**
   - 底部状态由长句改为“固定居中 · 分类与内容独立滚动”。
   - 小窗口仅显示必要的滚动保护提示。

## 配置兼容

- 不改变 v85 已新增的设置背景字段。
- 保留 v86 新增的 `experience.showSettingsDescriptions`。
- 旧配置中缺少该字段时默认显示描述，不改变原有使用习惯。
- 旧版无效右键菜单项目仍会在读取配置时自动过滤。
