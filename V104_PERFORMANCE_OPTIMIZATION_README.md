# v0.1.104 卡顿优化

本版本基于 v0.1.103 做性能专项优化，目标是降低项目数量较多、图标较多、频繁调整设置或使用彩虹鼠标效果时的主线程阻塞，同时避免改动高风险的原生贴边状态机。

## 1. 配置持久化降频

此前 Zustand persist 在每次状态更新后都会进入持久化流程。项目很多时，整份配置的 JSON 序列化与 localStorage 同步写入容易阻塞 UI，尤其是连续滑动设置、频繁交互时更明显。

v0.1.104 改为：

- 对持久化对象做顶层浅比较；纯 UI 临时状态变化不会重复落盘。
- 真正有持久化数据变化时，140ms 内连续修改合并为一次写入。
- `pagehide`、`beforeunload`、页面隐藏时强制刷新待写数据。
- 仍保持旧版 localStorage 数据格式兼容。

## 2. 快捷项目卡片事件监听优化

旧实现中每个 `ItemCard` 都会注册 4 个 window 级事件监听器。项目数量变大后，全局 pointer/mouse 事件会触发大量无关回调。

v0.1.104 改为共享 `pointerResetHub`：

- 全局只安装一组 pointerup / pointercancel / mouseup / blur 监听。
- 只有当前正在按下交互的卡片才加入重置集合。
- 正常情况下全局抬起一次只处理 1 个活跃卡片，而不是遍历所有卡片的独立监听。

## 3. 图标按可视区域懒加载

快捷项目不再在目录进入时一次性预解析全部图标。

- 使用 `IntersectionObserver`，项目进入或接近可视区域 320px 时才读取缓存/调用原生图标解析。
- HTTP、data URL 等直接图片同样延迟到接近可视区域再挂载，减少一次性图片解码。
- 图片使用 `loading="lazy"` 与 `decoding="async"`。
- “全部”聚合页的卡片启用 `content-visibility: auto`，减少离屏内容布局/绘制成本。

这项优化对几百个快捷项目、图标为本地 EXE/ICO 或 data URL 的配置最明显。

## 4. 图标缓存 localStorage 优化

- LRU 索引只在首次访问时读取到内存。
- 命中缓存时不再立即同步写回整个索引，改成短延迟批量刷新。
- 记录本会话不存在的持久化图标，避免同一路径反复查询 localStorage。
- 批量预加载接口改成分块处理，避免单个空闲回调中循环启动大量任务。

## 5. ItemCard 重渲染范围缩小

- `ItemCard` 使用 `React.memo` 自定义比较，只关注真正影响卡片显示/交互的 display、behavior 字段。
- 不再订阅完整 experience 对象，只订阅 tooltip 模式与启动次数角标。
- 选中状态判断由数组 `includes` 改成 memoized `Set`，多选或项目较多时避免重复线性查找。
- App 到 ContentArea 的右键菜单回调改成稳定 `useCallback`，避免父组件状态变化导致所有卡片失去 memo 命中。

## 6. 全局搜索重复计算缓存

全局搜索的中文归一化和拼音首字母转换会对大量项目重复计算。v0.1.104 对这两类结果增加有上限的内存缓存（最多约 12000 条，超限自动淘汰旧项），连续输入关键字时可以复用前一次计算结果。

## 7. 彩虹鼠标效果降重绘

旧版每次 `mousemove` 都会 `setCursor`，高轮询率鼠标可能让 React 每秒处理远高于屏幕刷新率的更新。

v0.1.104：

- 鼠标位置先写入 ref，每帧最多通过 `requestAnimationFrame` 提交一次。
- 光标位置直接更新 DOM 样式，不再为纯光标移动触发 React render。
- 只有开启拖尾时才更新拖尾状态。
- 为光标/拖尾增加合成器提示。

## 8. 图片浏览器大量缩略图优化

图片浏览器过去会在面板打开后让大量缩略图同时进入原生 `read_icon_as_data_url` 读取路径，并且面板宽度、缩略图栏宽度、裁剪指针移动都可能跟随高频 pointer 事件连续更新 React / 设置状态。

v0.1.104 改为：

- 缩略图使用 `IntersectionObserver`，只在进入或接近滚动区域约 240px 时读取图片数据。
- 同一路径的原生图片读取请求做 inflight 去重，预览区与缩略图不会重复并发读取同一张图。
- 图片 data URL 使用有上限的内存缓存（80 项），避免无限占用内存。
- 面板宽度与缩略图栏拖动改为每动画帧最多提交一次设置更新。
- 裁剪悬停与拖拽更新改为 `requestAnimationFrame` 节流，降低高轮询鼠标下的 React 更新次数。

这项优化主要改善图片库较大、快速滚动缩略图、拖动面板尺寸和裁剪图片时的卡顿。

## 未改动的高风险区域

本版没有重构 `edge_dock_native.rs`、原生窗口贴边/回弹状态机，也没有改变快捷项目启动判定语义。性能优化集中在前端主线程、缓存、持久化和渲染路径。

## 建议重点实机回归

1. 200~1000 个快捷项目的目录首次进入、滚动和切换。
2. EXE/文件图标较多的目录，观察进入页面时是否还有明显冻结。
3. 设置窗口连续拖动 UI 缩放、图标大小等滑块。
4. 单击/双击启动、长按拖动排序、右键菜单是否保持原行为。
5. 开启彩虹鼠标 + 拖尾后快速移动鼠标。
6. 全局搜索连续输入中文/拼音首字母。
7. 修改设置后正常退出并重新打开，确认配置正确持久化。
