# v76 更多体验设置与项目操作自由度

## 新增设置

### 启动后的行为

- 保持显示：维持之前的窗口状态。
- 最小化：项目启动成功后最小化 Yue launcher。
- 隐藏到托盘：项目启动成功后隐藏主窗口，可从托盘恢复。
- 启动后清除项目选中：避免刚打开的项目仍保持蓝色选中状态。
- 启动成功提示可独立开关。

### 键盘与搜索

- 方向键、Home、End 在当前可见项目之间移动。
- Enter 启动选中的项目。
- 直接输入文字时自动聚焦当前页搜索框。
- 当前页搜索可以选择是否同时匹配路径和网址。
- Ctrl+F 仍然可以直接聚焦当前页搜索。

### 项目信息

- 悬停提示可选：关闭 / 仅名称 / 详细。
- 详细提示包括项目路径、启动次数和最近启动时间。
- 可在项目右下角显示启动次数标记。
- 固定项目优先显示仍可独立开关。

### 快速预设

- 流畅效率
- 性能优先
- 防误操作
- 启动后收起

## 代码优化

- 新增 `lib/launchShortcut.ts`，统一项目卡片、右键菜单和全局搜索的项目启动行为。
- 新增 `lib/itemExperience.ts`，把搜索匹配、项目提示、键盘选择和启动后动作归一化做成可测试纯函数。
- 新增 4 项体验功能回归测试。
- 旧配置缺少新字段时自动使用安全默认值。

## 兼容性

- 默认启动后保持窗口显示，不强制最小化或隐藏。
- 原有单击/双击启动、拖动、右键菜单和全局搜索接口不变。
- localStorage 名称和导入导出数据结构保持兼容。
- 持久化版本更新为 7。
