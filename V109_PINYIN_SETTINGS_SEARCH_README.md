# v0.1.109 设置拼音搜索

本版本增强设置中心搜索，支持中文、完整拼音、拼音片段和拼音首字母。

## 新增能力

- `浏览器`：正常中文搜索。
- `liulanqi`：完整拼音可搜索“浏览器”。
- `liu` / `liulan`：输入部分拼音也可搜索“浏览器”相关设置。
- `llq`：拼音首字母可搜索“浏览器”。
- `pinyin`：可匹配“拼音”。
- `hh`：可匹配“行号”等设置词。
- 搜索同时覆盖设置分类、分类关键词和具体设置索引。
- 当具体设置命中时，左侧会保留它所在的设置分类，避免出现“具体结果有内容但左侧分类为空”的割裂体验。

## 实现说明

搜索匹配逻辑集中在 `src/lib/settingsSearch.ts`，由设置分类和具体设置索引共同复用。

`src/lib/settingsPinyinAliases.ts` 保存内置设置文本的无声调拼音与首字母索引，因此运行时不需要额外引入拼音 npm 依赖，也不会在用户每次输入时执行较重的拼音转换。

匹配优先级仍保持：精确匹配 > 前缀匹配 > 包含匹配。中文和英文关键词的原有搜索能力不受影响。

## UI 调整

设置搜索框提示更新为：

`搜索设置 / 拼音 / 首字母`

无结果提示中也增加了拼音示例，降低功能发现成本。
