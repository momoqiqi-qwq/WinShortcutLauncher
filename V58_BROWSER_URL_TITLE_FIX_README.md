# v58 浏览器拖入网址名称修复

## 修改内容

1. 拖动浏览器网页/标签页/收藏链接到 exe 时，添加为“网站”项目。
2. 网站名称优先使用浏览器拖拽数据里的显示名称/收藏名称。
3. 支持从 `text/x-moz-url`、`text/html`、`text/plain`、`DownloadURL`、`.url` 文件名等来源提取标题。
4. 如果拖拽数据里没有可用名称，添加后会尝试获取网页 `<title>` 并自动更新名称。
5. 避免把域名本身误当作名称，例如 `bd.wpjx.org` 不再作为最终名称优先使用。

## 版本

- 修改版：v58
- 程序版本：0.1.58

## 构建

已执行 `npm run build`，前端构建通过。当前环境没有 Cargo，所以 Rust/Tauri 原生编译未执行。
