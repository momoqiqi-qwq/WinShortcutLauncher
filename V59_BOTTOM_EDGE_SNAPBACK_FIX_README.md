# v59 底部贴边与拖出屏幕弹回修复

## 修改内容

1. 修复底部贴边隐藏闪烁
   - 底部贴边被鼠标触发展开后，如果鼠标仍在主界面内，不会立刻再次触发隐藏。
   - 底部收回后会要求鼠标先离开保留边缘，再允许下一次触发展开，减少“刚弹出又收回、反复弹出”的循环。

2. 新增拖出屏幕弹回设置
   - 设置 - 窗口行为 新增“窗口拖出屏幕时弹回屏幕内”。
   - 开启后，把 exe 界面一部分拖到屏幕外，会弹回屏幕内。
   - 左侧超出时，窗口左边缘会对准屏幕左边缘。
   - 鼠标仍在 exe 界面内时，不会触发拖出屏幕自动隐藏。

3. 拖动窗口时不自动隐藏
   - 鼠标左键拖动窗口期间，不触发拖出屏幕隐藏/弹回，松开后再按设置判断。

4. 版本更新
   - 修改版：v59
   - 程序版本：0.1.59

## 构建

已执行 `npm run build`，前端构建通过。当前环境没有 Cargo，Rust/Tauri 原生编译未执行。
