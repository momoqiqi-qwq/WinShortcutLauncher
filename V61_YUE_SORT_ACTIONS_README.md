# v61 Yue launcher / 父目录排序 / 右上角功能显示

本版修改：

1. EXE / 窗口名称
   - productName 改为 `Yue launcher`
   - 主窗口标题改为 `Yue launcher`
   - 托盘提示、Windows 查找窗口标题同步改为 `Yue launcher`
   - 程序版本更新为 `0.1.61`

2. 右上角新增父目录按字母排列按钮
   - 新增 `A-Z` 排序按钮。
   - 点击后会把顶部父目录按名称排序。
   - 支持中文/英文/数字混合排序。
   - 排序结果会写入父目录 order，重启后保留。

3. 设置中控制右上角功能显示
   - 设置 - 界面 - 右上角功能按钮 中新增“右上角功能是否显示”。
   - 可单独控制：全局搜索、文件中转站、图片预览、父目录 A-Z 排列、新增父目录、设置、置顶、最小化、关闭。
   - 提供“全部显示”和“只留基础按钮”。
   - 隐藏按钮只影响显示，不破坏按钮顺序；按钮顺序仍可在主界面拖动调整。

已执行 `npm run build`，前端构建通过。
当前环境没有 Cargo，所以 Rust/Tauri 原生编译未执行。
