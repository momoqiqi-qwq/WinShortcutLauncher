# V85 设置居中、右键菜单与独立背景优化

版本：`0.1.85`

## 本次修改

1. **说明改为关于**
   - 设置分类“说明”改为“关于”。
   - 页面只保留版本、作者 `momoni_yile`、框架和项目地址。
   - 项目地址支持打开和复制：`https://github.com/momoqiqi-qwq/WinShortcutLauncher`

2. **设置窗口体验优化**
   - 设置窗口固定在主界面正中央，不再支持拖动或自由缩放。
   - 左侧分类和右侧内容分别滚动，分类较多时不会被截断。
   - 搜索框保持在分类顶部，并保留分类滚动位置和返回顶部功能。
   - 设置界面缩放支持 `x0.1` 到 `x1.8`，步进为 `x0.1`。

3. **主窗口最小尺寸保护**
   - exe 主窗口最小尺寸调整为 `900 × 640`。
   - 历史保存尺寸低于安全值时自动恢复为默认 `1180 × 880`。
   - Tauri 配置、前端窗口状态校验和 Rust 窗口恢复逻辑使用相同限制。

4. **三套独立右键菜单**
   - 设置 - 右键菜单只保留：`父目录`、`子目录`、`空白处`。
   - 父目录标签、子目录标签、侧栏或内容空白处分别打开不同菜单。
   - 子目录菜单只保留空白处菜单没有的管理功能：重命名、合并、类型切换、清空、删除。
   - 空白处菜单负责新建子目录、添加内容、显示设置和刷新图标。
   - 父目录菜单负责新建、合并和删除父目录。

5. **父目录合并和删除修复**
   - 补回全局确认/输入弹窗宿主，修复确认框无法完成而导致的父目录删除、合并无响应问题。
   - 合并目标子菜单阻止事件冒泡，避免点选目标时被父级菜单抢先处理。

6. **设置背景与主界面背景分离**
   - 新增独立的设置界面背景开关、图片路径、透明度、明暗、模糊、填充方式、位置和面板不透明度。
   - 设置背景只作用于设置窗口，不会覆盖主界面背景。

## 额外用户体验改进

- 右键菜单项目名称明确标注“父目录”或“子目录”，减少误操作。
- 新建子目录改为普通、全部、便签三种子菜单入口。
- 右键菜单可按位置独立隐藏项目，并提供显示数量和常用精简配置。
- 设置分类按钮补充可访问性状态和悬停标题。
