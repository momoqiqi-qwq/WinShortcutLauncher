# v0.1.110 拼音搜索扩展

本版本把 v0.1.109 的设置拼音搜索扩展到运行时数据：主界面项目搜索、全局搜索 / 命令面板，以及父目录和子目录名称都可以使用中文、完整拼音、拼音片段和首字母搜索。

## 新增能力

### 主界面项目搜索

项目名称现在支持：

- `浏览器` → 中文直接匹配。
- `liulanqi` → 完整拼音匹配“浏览器”。
- `liu` → 拼音前缀匹配“浏览器”。
- `llq` → 拼音首字母匹配“浏览器”。
- `ai` → 可匹配 `AI 工具`，也可匹配以 `ai...` 开头拼音的中文名称。

原有“搜索路径 / 网址”能力继续保留；关闭路径搜索后，拼音仍只匹配项目名称和类型，不会绕过原设置去搜索路径。

### 全局搜索 / 命令面板

当前工程中的“全局搜索”和“命令面板”共用同一个搜索面板，本版本统一增强了其匹配引擎：

- 项目名称：完整拼音 + 拼音片段 + 首字母。
- 父目录名称：完整拼音 + 首字母。
- 子目录名称：完整拼音 + 首字母。
- 设置、命令、主题、字体等已有条目也继续受拼音匹配支持。
- `搜索父目录` 与 `搜索子目录` 两个开关现在分别作用于独立字段，不再把父 / 子目录拼在同一个 subtitle 中混合判断。

同时新增“父目录”结果类型。搜索到父目录后执行结果会直接切换到对应父目录；搜索到子目录则会定位到对应父目录和子目录。

示例：

- `工作` / `gongzuo` / `gz` → 父目录“工作”。
- `文件夹` / `wenjianjia` / `wjj` → 子目录“文件夹”。
- `浏览器` / `liulanqi` / `llq` → 项目“浏览器”。

## 共享拼音引擎

新增：

- `src/lib/pinyinSearch.ts`
- `src/lib/pinyinSyllables.ts`

`pinyinSearch.ts` 负责：

- 搜索文本归一化。
- 运行时中文名称转无声调完整拼音。
- 运行时中文名称转拼音首字母。
- 精确 / 前缀 / 包含匹配。
- 常见多音词短语纠正。
- 搜索转换结果缓存。

`pinyinSyllables.ts` 是通过 ICU Han-Latin 转写生成的常用 CJK 字符拼音表，使用紧凑的分隔字符串保存。运行时不需要新增拼音 npm 依赖。

v0.1.109 的 `settingsPinyinAliases.ts` 仍然保留，并作为内置设置词条的额外别名层；设置搜索现在也复用共享匹配引擎。

## 兼容性

- 不新增 npm runtime dependency。
- 保留 v0.1.109 命令面板的 `mmlb` 兼容别名。
- 中文、英文、路径和网址的原有搜索继续可用。
- 全局搜索的“启用拼音”开关现在同时控制完整拼音和首字母匹配。
