# v0.1.100 优先稳定性修复

## 1. 旧配置与导入配置

`normalizeGroups()` 现在接收任意输入，并在每一级执行明确的数组校验。新增 `normalizeShortcutItem()` 作为项目数据唯一清洗入口：

- 非字符串名称、路径和图标转换为安全字符串。
- 缺失或重复 ID 自动生成新 ID。
- 非法项目类型回退为文件，网址路径可自动识别为 URL。
- 顺序、标签行数、启动次数和最近启动时间统一做有限数值校验。
- 每个父目录至少保留一个可用子目录。

持久化恢复和 JSON 配置导入均先检查根节点是否为普通对象，再进入归一化流程。

## 2. 全局命令面板按需加载

主界面只在 `globalSearchOpen` 为真时挂载全局命令面板；面板组件自身也保留关闭状态快速返回。搜索索引仅在打开面板后创建，关闭期间项目变化不会触发隐藏索引重建。

## 3. 壁纸导入与动态媒体

- 本地文件先检查类型、大小和缓存状态，再复制到应用数据目录。
- 复制失败时保留原设置并显示失败提示，不再把桌面或下载目录路径误当成已应用资源。
- 256 MB 及以上文件需要二次确认，2 GB 以上文件拒绝导入。
- 文件复制通过 Tauri 异步命令中的阻塞任务执行。
- PNG/APNG 检查 `acTL` 块，WebP 检查 `ANIM`/`ANMF` 块；检测失败时使用保守的“可能包含动画”提示。
- 减少动态效果或暂停预览时，图片媒体绘制为单帧 Canvas。

## 4. 设置性能和快捷入口

- 路径输入不会边输入边加载；点击应用或按 Enter 后才生效。
- 滑块 160 ms 防抖，并在松开、键盘操作结束或失焦时立即提交。
- 预览焦点拖动只更新局部状态，松开时写入一次配置。
- 动态预览默认暂停，避免设置背景和多个预览同时播放。
- 快捷设置菜单根据触发按钮位置计算坐标，并补齐菜单键盘导航。

## 5. 构建约束

`package.json` 已声明 Node.js `^20.19.0 || >=22.12.0`。Windows 构建和验证脚本会检查版本，开发构建统一使用 `npm ci`。

本版本没有伪造 `Cargo.lock`。首次在具备 Rust/Cargo 的联网环境成功构建后，应提交 Cargo 自动生成的锁文件，以锁定 Rust 依赖解析结果。

## 尚未纳入本次优先修复

- 大文件复制进度与取消按钮。
- 壁纸缓存容量列表、未使用缓存识别和一键清理。
- 壁纸库、播放列表和定时切换。
- `Cargo.lock`：必须由安装了 Rust/Cargo 的环境真实解析生成，本包未手工伪造。

这些功能适合在完成 Windows 实机回归后继续加入，避免在无法编译 Rust 的环境中一次引入过多原生命令风险。
