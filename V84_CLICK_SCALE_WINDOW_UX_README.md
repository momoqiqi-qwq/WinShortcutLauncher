# V84 点击、缩放与最小窗口体验优化

## 本次修改

1. 主界面和设置界面缩放最低固定为 100%，最高 180%。
2. Ctrl + 滚轮、Ctrl + +/-、Ctrl + 0 全部走统一缩放逻辑，无法再把界面缩得过小。
3. 主窗口正常状态最小尺寸调整为 720 × 520；过小的历史窗口快照会自动回退到默认大小。
4. 左键不再选中项目，普通左键会清除旧选择状态：
   - 单击模式：单击启动，长按后拖动排序。
   - 双击模式：单击无动作，双击启动，拖动排序。
5. 右键用于管理当前项目，Ctrl + 右键保留多选；键盘导航和批量管理能力继续保留。
6. 项目提示中明确展示当前启动规则，并增加键盘焦点和按压反馈。
7. 设置页增加实时规则说明，切换单/双击后显示确认提示。

## 兼容说明

贴边隐藏仍采用移动完整窗口或独立触发条的方式，不依赖把正常主界面缩成极小窗口，因此不会破坏现有贴边功能。
