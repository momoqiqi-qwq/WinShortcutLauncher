# v72 代码结构检查与下一步建议

本版只执行低风险拆分和体验优化，没有直接进行大规模重构。

## 已处理

- 新的体验设置单独放入 `ExperienceSettingsSection.tsx`，避免继续把所有设置写进 `SettingsPanel.tsx`。
- 项目排序规则集中在 `lib/sort.ts`。
- 项目使用统计由 store 统一记录，不在多个界面分别维护。
- 体验配置加入统一默认值、归一化、迁移、导入、导出和持久化。
- 非自定义排序禁用拖拽，减少状态和界面行为不一致。

## 发现的主要结构问题

1. `SettingsPanel.tsx` 仍然过大，图标、窗口行为、数据、帮助等页面继续集中在一个文件。
2. `appStore.ts` 同时负责配置、导航、项目、中转、图片、导入与迁移，后续容易发生版本覆盖或漏合并。
3. `App.tsx` 同时处理网页拖入、全局快捷键、贴边暂停、浮层和窗口样式，职责过多。
4. 贴边隐藏由前端 hooks 与 Rust 原生控制器共同参与，状态来源不完全唯一。
5. 当前没有自动化测试，Maye/Lucy 导入、favicon 主域名复用、配置迁移和排序容易在后续版本回退。

## 建议征求确认后再做

### 方案 A：安全结构拆分

- 把 SettingsPanel 按分类拆成独立文件。
- 把 appStore 拆成 navigation/items/settings/media 四个 slice。
- 不改变界面和数据格式，风险最低。

### 方案 B：自动化回归测试

- 增加 Vitest。
- 测试 favicon 主域名、Maye/Lucy 导入、配置迁移、项目排序、窗口尺寸异常恢复。
- 每次打包前自动运行，最适合防止“上一版功能下一版消失”。

### 方案 C：贴边隐藏状态机统一

- 把正常、隐藏、展开、回弹、任务栏唤醒统一成单一状态机。
- 体验提升明显，但涉及 Rust 和窗口时序，风险高于 A/B。

### 方案 D：项目数据增强

- 增加最近使用虚拟标签、收藏标签、失效路径检查、批量修复路径和重复项目检测。
- 功能价值高，但需要扩展数据模型与界面。
