# v76 代码结构检查与后续建议

## 本版已经处理

1. 项目启动行为原来分散在项目卡片、项目右键菜单和全局搜索中，本版统一到 `lib/launchShortcut.ts`。
2. 键盘选择、搜索匹配和项目提示拆成纯函数，并加入自动化测试。
3. 新体验设置全部经过默认值、旧配置归一化和 Zustand 持久化。

## 仍值得优化的地方

### 1. SettingsPanel.tsx 仍然过大

当前设置主面板约 1300 行。虽然彩虹、便签、体验、自检等已经拆出，但操作、拖动、窗口、图标、数据和帮助仍集中在一个文件。

建议下一步按设置分类拆成独立组件，减少修改一个设置页时影响其他设置页的概率。

### 2. App.tsx 仍承担过多全局职责

App.tsx 同时负责：

- 浏览器网页拖入
- 全局快捷键
- 所有浮层开关
- 贴边暂停与恢复
- 通知气泡
- 右键菜单路由
- 主窗口拖动

建议拆成 `useGlobalShortcuts`、`useOverlayController`、`useBrowserUrlDrop` 和 `AppOverlays`。

### 3. 贴边逻辑仍分散

贴边隐藏涉及多个前端 Hook 和 Rust 原生轮询。后续功能继续增加时，仍可能出现底部反复展开、任务栏唤醒和回弹互相影响。

建议单独做一个贴边状态机版本，并为状态转换增加测试。

### 4. 快捷键目前是固定的

v76 增加了键盘导航，但快捷键仍是固定值。下一步可以增加快捷键编辑器、冲突检测和恢复默认功能。

### 5. 缺少真实 Windows EXE 端到端测试

当前纯函数、配置迁移和前端生产构建已有自动化测试；窗口置顶、托盘、贴边、关机状态保存和原生图标仍需要 Windows WebDriver 或人工回归清单。

## 推荐下一版本

优先推荐 v77 做“设置页安全拆分 + 自定义快捷键”。

原因：

- 设置页已经是当前最容易互相覆盖的文件。
- 自定义快捷键能直接提升使用效率和自由度。
- 两项可以一起做：拆分设置页时顺便新增独立“快捷键”分类。

贴边状态机建议放到之后单独一个版本，避免与普通界面设置同时大改。
